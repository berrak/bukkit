README DEBIAN WHEEZY
====================

To create and load data, run files in /var/lib/postgresql
which is the $HOME for user 'postgres'. Run these as
as user postgres (i.e. do # su - postgres).

1. Create the database itself:

# su - postgres

$ psql -f create_database_openjensen.sql

2. Create the 't_ort' table and insert some data:

$ psql -d openjensen -U jensen -f mk_tbl_tort.sql
$ psql -d openjensen -U jensen -f insert_tort.sql

3. There may be other similar sql's for tables/data inserts

All sql files of this type is named: mk_tbl_* and insert_*


